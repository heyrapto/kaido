# Kaido no longer needs a server-side Gemini key — name generation runs in
# the browser via Puter.js (https://puter.com), which uses a "user-pays"
# model (each visitor's Puter account covers their own usage).
#
# NINJA_API_KEY is no longer required either — domain availability uses the
# free Verisign RDAP service.